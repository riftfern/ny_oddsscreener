{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @ny-sharp-edge/web run build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
