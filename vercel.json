{
  "installCommand": "pnpm install",
  "buildCommand": "cd apps/web && pnpm run build && cp -r dist ../../",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
