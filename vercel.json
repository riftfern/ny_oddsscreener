{
  "installCommand": "pnpm install",
  "buildCommand": "pwd; ls -la; pnpm run vercel-build; ls -la apps/web/; ls -la apps/web/dist/ || echo 'no dist folder'",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
