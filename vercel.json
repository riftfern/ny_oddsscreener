{
  "buildCommand": "cd apps/web && pnpm run build && ls -la && ls -la dist/",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
