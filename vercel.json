{
  "buildCommand": "pnpm --filter @ny-sharp-edge/shared run build && pnpm --filter @ny-sharp-edge/web run build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
